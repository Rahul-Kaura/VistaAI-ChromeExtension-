<svg width="128" height="128" viewBox="0 0 128 128" xmlns="http://www.w3.org/2000/svg">
  <!-- Background circle -->
  <circle cx="64" cy="64" r="60" fill="#000000"/>
  
  <!-- Outer ring segments -->
  <!-- Red segment (top-right) -->
  <path d="M 64 4 A 60 60 0 0 1 120 64 L 100 64 A 40 40 0 0 0 64 24 Z" fill="#EA4335"/>
  
  <!-- Yellow segment (bottom-right) -->
  <path d="M 120 64 A 60 60 0 0 1 64 124 L 64 104 A 40 40 0 0 0 100 64 Z" fill="#FBBC04"/>
  
  <!-- Teal/Green segment (left) -->
  <path d="M 64 124 A 60 60 0 0 1 4 64 L 24 64 A 40 40 0 0 0 64 104 Z" fill="#34A853"/>
  
  <!-- Inner lens/pupil -->
  <circle cx="64" cy="64" r="25" fill="url(#lensGradient)"/>
  
  <!-- Highlight -->
  <circle cx="58" cy="58" r="8" fill="#FFFFFF" opacity="0.8"/>
  
  <!-- Gradient definition for lens -->
  <defs>
    <radialGradient id="lensGradient" cx="50%" cy="50%" r="50%">
      <stop offset="0%" style="stop-color:#4285F4;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#1A73E8;stop-opacity:1" />
    </radialGradient>
  </defs>
</svg>
